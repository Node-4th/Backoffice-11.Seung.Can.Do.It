<body>
    <%- include('tutor_header.ejs') %>

        <h1 class="createC">피드백 작성</h1>
        <form class="form-box" action="/feedbacks/<%= taskId %>" method="POST">
            <div class="ta">
                <h3>title</h3>
                <input class="feed_input" type="text" name="title" placeholder="title을 입력하세요">
                <h3>content</h3>
                <textarea class="feed_input2" name="content" placeholder="content을 입력하세요"></textarea>                <h3>평점</h3>
                <select class="feed_input3" name="rating">
                    <% for (let i=1; i <=10; i++) { %>
                        <option value="<%= 'RATING_' + i %>">
                            <% for (let j=0; j < i; j++) { %>
                                <% } %>
                                    <%= i %>
                        </option>
                        <% } %>
                </select>
                <button class="project-btn">생성하기</button>
            </div>
        </form>
</body>